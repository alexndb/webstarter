mixin button(text, mods)
  -
    var allMods = '';
    if (typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' button--' + modsList[i].trim();
      }
    }
    
  if (attributes.href)
    +b.A.button(class=`${allMods}`)&attributes(attributes)= text
  else
    +b.BUTTON.button(class=`${allMods}`)&attributes(attributes)= text